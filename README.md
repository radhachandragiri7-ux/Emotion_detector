Emotion_detector/
│
├── app.py
│   → main Flask backend file
│   → handles routes, emotion detection logic, and chart rendering
│
├── requirements.txt
│   → contains all Python libraries required (Flask etc.)
│
├── history.json
│   → stores emotion detection history (optional)
│
├── templates/
│   └── index.html
│       → front-end UI for user input, emotion display, and chart visualization
│
├── static/
│   ├── style.css        (if you made any CSS for styling)
│   ├── script.js        (if you added custom JS)
│   └── assets/          (optional folder for images or icons)
│
└── README.txt
    → short project description (optional but good for submission)
# Emotion_detector
